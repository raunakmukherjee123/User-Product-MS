spring:
  application:
    name: PracticeMicroservice
  datasource:
    url: jdbc:mysql://localhost:3306/user_ms
    username: root
    password: 1234@root
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true
