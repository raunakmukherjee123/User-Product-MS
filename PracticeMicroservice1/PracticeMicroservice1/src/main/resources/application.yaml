spring:
  application:
    name: PracticeMicroservice1
  datasource:
    url: 
    username: 
    password: 
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone:
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true
